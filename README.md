# RESTful API with Node.js, Express, and MongoDB

## Overview

This project builds a backend system to serve and analyze city demographic data using Node.js, Express, and MongoDB.

## Setup Instructions

1. Clone the repository
2. Run `npm install` to install dependencies
3. Set up nongoDB locally or use MongoDB Atlas
4. Create a `.env` file with your MongoDB URI